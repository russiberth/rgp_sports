server:
  port: 8080

spring:
  datasource:
    url: jdbc:h2:mem:projetodb
    username: sa
    password:

  h2:
    console:
      enabled: true 
      path: /h2-console

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

springdoc:
  swagger-ui: 
    path: /swagger-ui.html